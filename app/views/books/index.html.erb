<!--<script src="/raty.js"></script>-->

<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'users/info', user: @user, following_users: @following_users, follower_users: @follower_users %>
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @book %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>Books</h2>
      <p>
        <%= link_to '新しい順', books_path(latest: "true") %> |
        <%= link_to '古い順', books_path(old: "true") %> |
        <%= link_to '評価の高い順', books_path(rate_count: "true") %>
      </p>
      <%= render 'index', books: @books, post_comments: @post_comments %>
    </div>
  </div>
</div>

<!--, favorites: @favoritesはアソシエーションにより、定義する必要がないため不要-->

<!--publicはルートパスになる-->
<!--weppackではコンパイルの仕様の仕方が違うことによりエラーになる
バージョンの違いによるものであるため->
<!--そのため直接読み込むようにする　該当のビューで直接読み込むようにする-->



<!--weppack rails6から変わる-->